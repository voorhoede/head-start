---
import type { Code } from 'datocms-structured-text-utils';

interface Props {
  node: Code;
}

const { node } = Astro.props;
const { code, language } = node;
---

<pre data-language={language || undefined}><code>{code}</code></pre>
