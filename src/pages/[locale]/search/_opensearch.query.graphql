query OpenSearchXml(
  $locale: SiteLocale!, 
  $fallbackLocales: [SiteLocale!]
) {
  site: _site(locale: $locale, fallbackLocales: $fallbackLocales) {
    globalSeo {
      fallbackSeo {
        description
      }
    }
    favicon {
      url
    }
  }
}
