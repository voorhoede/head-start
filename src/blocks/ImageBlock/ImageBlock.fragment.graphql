fragment ImageBlock on ImageBlockRecord {
  id
  image {
    alt
    title
    width
    height
    url
    responsiveImage( imgixParams: { auto: [format, compress] } ) {
      srcSet
      src
      base64
      alt
      title
      width
      height
      sizes
    }
  }
}