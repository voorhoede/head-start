---
import type { ImageBlockFragment } from '@lib/datocms/datocms.d.ts';
import Image from './Image.astro';

export interface Props {
  block: ImageBlockFragment
}

const { block } = Astro.props;
---

<Image image={ block.image } />
