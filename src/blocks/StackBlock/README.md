# Stack Block

**Renders items stacked one after the other.**

Enable "Show titles" to display each item's heading above its content. Leave it off to render items without any headings, flowing directly one after another.
