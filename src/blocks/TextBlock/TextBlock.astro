---
import type { TextBlockFragment } from '../../lib/types/datocms';
import StructuredText from '../../components/StructuredText/StructuredText.astro';

interface Props {
  block: TextBlockFragment
}
const { block } = Astro.props;
---

<StructuredText content={block.text} />
