---
import type { VariableBlockRecord } from '~/lib/datocms/schema';

export interface Props {
  block: VariableBlockRecord;
}

const { block } = Astro.props;
const variable = block.variable;
---

{variable && <span data-variable={variable.value}>{variable.value}</span>}
