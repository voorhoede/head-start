# Column Block

**Renders items side by side in a multi-column grid.**

Choose 2, 3, or 4 columns. Each item's nested blocks are rendered inside its own column. On narrow screens the columns collapse into a single column.
