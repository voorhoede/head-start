import { describe, expect, test } from 'vitest';
import { renderToFragment } from '@lib/renderer';
import {{ pascalCase name }}, { type Props as {{ pascalCase name }}Props } from './{{ pascalCase name }}.astro';

const fragment = await renderToFragment<{{ pascalCase name }}Props>({{ pascalCase name }});

describe('{{ pascalCase name }}', () => {
  test('Block is rendered', () => {
    expect(fragment).toBeDefined();
  });

  // Add more tests here
});
